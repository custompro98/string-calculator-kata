import { add } from "../string_calculator"

describe("StringCalculator", () => {
  describe("add", () => {
    it("is currently failing", () => {
      expect(true).toBe(false)
    })

    it("can find the method", () => {
      add("")
    })
  })
})
